#include <fstream>

using namespace std;

/**
 * reads the file contents and populates internal structures with the relevant information
 */
void importFile(fstream &f);

int main(int argc, char** argv);